[project]
name = "stub-app-service-porc-cloud"
version = "0.0.1"
description = "FastAPI service for collecting service information"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]",
    "httpx",
]